info:
  name: delete-project-boundary
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/projects/:id"
  params:
    - name: id
      value: "{{boundaryProjectId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
