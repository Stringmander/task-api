info:
  name: delete-project
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/projects/:id"
  params:
    - name: id
      value: "{{projectId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
