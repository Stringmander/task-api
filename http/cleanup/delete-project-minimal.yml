info:
  name: delete-project-minimal
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/projects/:id"
  params:
    - name: id
      value: "{{minimalProjectId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
