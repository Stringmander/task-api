info:
  name: update-task-multiple-fields
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/tasks/:id"
  params:
    - name: id
      value: "{{taskId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "in_progress",
        "position": 2,
        "description": "Reordered and reopened"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
