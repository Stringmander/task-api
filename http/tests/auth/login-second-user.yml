info:
  name: login-second-user
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "demo2@example.com",
        "password": "correcthorse"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setVar("secondUserAccessToken", res.body.accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
