info:
  name: create-project-cascade-delete
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/projects"
  body:
    type: json
    data: |-
      {
        "name": "Cascade Delete Project"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("cascadeProjectId", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
