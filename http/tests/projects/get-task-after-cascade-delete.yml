info:
  name: get-task-after-cascade-delete
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/tasks/:id"
  params:
    - name: id
      value: "{{cascadeTaskId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
