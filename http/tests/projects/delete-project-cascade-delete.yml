info:
  name: delete-project-cascade-delete
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/projects/:id"
  params:
    - name: id
      value: "{{cascadeProjectId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
