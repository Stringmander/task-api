info:
  name: list-projects-garbage-token
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/projects"
  auth:
    type: bearer
    token: garbagetoken

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
