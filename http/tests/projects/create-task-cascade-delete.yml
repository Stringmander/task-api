info:
  name: create-task-cascade-delete
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/projects/:id/tasks"
  params:
    - name: id
      value: "{{cascadeProjectId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Task that should disappear with its project"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("cascadeTaskId", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
