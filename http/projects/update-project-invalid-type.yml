info:
  name: update-project-invalid-type
  type: http
  seq: 10

http:
  method: PATCH
  url: "{{baseUrl}}/projects/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": 12345
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
