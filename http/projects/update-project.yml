info:
  name: update-project
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{baseUrl}}/projects/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Renamed Project"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
