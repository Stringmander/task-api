info:
  name: create-project-minimal
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/projects"
  body:
    type: json
    data: |-
      {
        "name": "Minimal Project"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("minimalProjectId", res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
